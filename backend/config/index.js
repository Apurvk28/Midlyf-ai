// Placeholder for runtime configuration (e.g., API keys, database URLs)
// require('dotenv').config(); // Uncomment when using dotenv

module.exports = {
  port: process.env.PORT || 5001,
  // hfApiKey: process.env.HF_API_KEY
};
